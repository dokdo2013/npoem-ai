{
  "name": "npoem-ai",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "web": "turbo run --filter=@npoem-ai/web",
    "api": "turbo run --filter=@npoem-ai/api",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.1.1",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}
